apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.hpaDeployment.name }}-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.hpaDeployment.name }}
  minReplicas: {{ .Values.hpaDeployment.hpa.minReplicas }}
  maxReplicas: {{ .Values.hpaDeployment.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpaDeployment.hpa.targetCPUUtilizationPercentage }}
